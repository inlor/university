<!DOCTYPE html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MemoColor</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="/assets/css/style.css">
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="<%=url('home')%>">MemoColor</a>
            <% if (locals.auth) {%> 
            <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="<%=url('dashboard')%>">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="<%=url('create')%>">Create</a>
                    </li>
                </ul>
            </div>
            <% } %>

            <div class="justify-content">
                <% if (!locals.auth) {%> 
                <a href="<%=url('sing-in')%>" class="btn btn-outline-success">Login</a>
                <a href="<%=url('sing-up')%>" class="btn btn-outline-primary">Register</a>
                <% } else { %>
                <form class ="logout-form" method="POST" action="<%=url('logout')%>">
                    <input class="btn btn-primary" type="submit" value= "Logout">
                </form>
                <% } %>
            </div>
        </div>
    </nav>
    <img id="img-bg" src="/assets/img/bg.png" alt="background">